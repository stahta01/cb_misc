Building wxWidgets using MSys2 packaging.txt

See https://wiki.archlinux.org/index.php/PKGBUILD
    https://www.archlinux.org/pacman/PKGBUILD.5.html

# Open MSys2 command prompt


# One time build tools installation
pacman -S base-devel
# for building mingw32 packages
pacman -S mingw-w64-i686-toolchain
# for building mingw64 packages
pacman -S mingw-w64-x86_64-toolchain

Some of the options
  -R, --repackage  Repackage contents of the package without rebuilding
  -s, --syncdeps   Install missing dependencies with pacman
  -L, --log        Log package build process
  -f, --force      Overwrite existing package
  -o, --nobuild    Download and extract files only
  -p <file>        Use an alternate build script (instead of 'PKGBUILD')
  --nocheck        Do not run the check() function in the PKGBUILD
  --verifysource   Download source files (if needed) and perform integrity checks
  -c, --clean      Clean up work files after build
  -C, --cleanbuild Remove $srcdir/ dir before building the package
  -d, --nodeps     Skip all dependency checks
  -e, --noextract  Do not extract source files (use existing $srcdir/ dir)


# Change to package(s) folder
cd C:/Devel/open_source_code/version_control/MINGW_packages-git/mingw-w64-wxwidgets3.1-git
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf                               # Package was built and installs
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf -p wxgtk3.1-gtk2-git.pkgbuild # Building
                                                                          NOTE: NON-GUI Tests Segfaults
                                                                          Add no xtest configure option
                                                                          Remove building of samples and tests
Repackage
MINGW_INSTALLS=mingw32 makepkg-mingw -RLf -p wxgtk3.1-gtk2-git.pkgbuild #

# Change to package(s) folder
cd C:/Devel/open_source_code/version_control/MINGW_packages-git/mingw-w64-wxwidgets3.1
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf                               # 
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf -p wxgtk3.1-gtk2.pkgbuild     # 


# Change to package(s) folder
cd C:/Devel/open_source_code/version_control/MINGW_packages-git/mingw-w64-wxwidgets3.0-git
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf                               # 
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf -p wxgtk3.0-gtk2-git.pkgbuild # 

# Change to package(s) folder
cd C:/Devel/open_source_code/version_control/MINGW_packages-git/mingw-w64-wxwidgets
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf                               # 
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf -p wxgtk3.0-gtk2.pkgbuild     # 

# Change to package(s) folder
cd C:/Devel/open_source_code/version_control/MINGW_packages-git/mingw-w64-wxwidgets2.8-git
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf                               # Package was built and installs
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf -p wxgtk2.8-gtk2-git.pkgbuild # Failed

In file included from ./src/regex/regcustom.h:39:0,
                 from ./src/regex/regguts.h:38,
                 from ./src/regex/regexec.c:32:
./include/wx/defs.h:2825:20: error: conflicting types for 'WXWidget'
 typedef GtkWidget *WXWidget;
                    ^~~~~~~~
./include/wx/defs.h:2616:25: note: previous declaration of 'WXWidget' was here
 typedef WXHWND          WXWidget;
                         ^~~~~~~~


# Build the 32 bit and 64 packages
# makepkg-mingw -sLf

# Build only the 32 bit package
MINGW_INSTALLS=mingw32 makepkg-mingw -sLf

# Build only the 64 bit package
#  MINGW_INSTALLS=mingw64 makepkg-mingw -sLf


# Look for the packages
ls mingw-w64-*-*-*-*.pkg.tar.xz

pacman -Qs wxwidgets

# Install a 'local' 32 bit package
pacman -U mingw-w64-i686-wxWidgets2.8-2.8.12-1-any.pkg.tar.xz

# Install a 'local' 64 bit package
pacman -U mingw-w64-x86_64-wxWidgets2.8-2.8.12-1-any.pkg.tar.xz


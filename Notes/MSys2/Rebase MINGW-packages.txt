$ git branch --list
  cleanup
  editors_and_IDEs
  master
  misc
  scratch
  staging
  testing
* unit_tests
  wxApps
  wxwidgets

git checkout master && git fetch upstream && git rebase upstream/master && git push origin

git checkout cleanup && git pull && git rebase --ignore-date master && git push --force-with-lease
git status


git checkout staging && git pull && git rebase master && git push
#   git rebase -i master   ## Keep ONLY changes I wish to push upstream
#   git push --force-with-lease


git checkout wxwidgets && git pull && git rebase master && git status -uno
git rebase -i master
git rebase --ignore-date master
git push --force-with-lease

git checkout editors_and_IDEs && git pull && git rebase master && git status -uno
git rebase -i master
git rebase --ignore-date master
git push --force-with-lease

git checkout wxApps && git pull && git rebase master && git status -uno
git rebase -i master
git push --force-with-lease


git checkout testing && git pull && git status -uno
#
git rebase cleanup && \
git rebase staging && \
git rebase editors_and_IDEs && \
git rebase master && git status -uno
#
git rebase -i master
git push
# git push --force-with-lease


##
git checkout scratch && git pull && git status -uno
#
git rebase cleanup && \
git rebase staging && \
git rebase wxwidgets && \
git rebase wxApps && \
git rebase editors_and_IDEs && \
git rebase master && git status -uno
#
git rebase -i master
git push
#   git push --force-with-lease


git checkout unit_tests && git pull && git rebase master && git status -uno
#
git rebase master && git status -uno
git rebase -i master
git push
# git push --force-with-lease


git checkout misc && git pull && git rebase master && git status -uno
#
git rebase master && git status -uno
git rebase -i master
git push
# git push --force-with-lease

####
# Requires Cygwin packages:
#   libgtk2.0-devel
#
####

$ pkg-config gtk+-2.0 --libs


$  cd ~/devel/wxWidgets/wxWidgets

$  mkdir build_gtk2_wx3.1.x_shared_unicode
$  cd build_gtk2_wx3.1.x_shared_unicode
$  export CPPFLAGS=""
$  export CXXFLAGS="-Wno-attributes"
# Do NOT use host unless it differs from build and maybe not even then --host=i686-pc-cygwin \
$  ../configure --prefix=/opt/wx/gtk2/3.1.x \
      --with-gtk=2 \
      --disable-monolithic \
      --enable-shared \
      --enable-xrc \
      --with-libjpeg=builtin \
      --with-regex=builtin \
      --with-tiff=builtin \
      --without-gtkprint \
      --without-gnomeprint \
      --disable-universal \
      --disable-precomp-headers \
      --enable-unicode


###
  Which GUI toolkit should wxWidgets use?                 GTK+ 2 with support for libnotify
  Should wxWidgets be compiled into single library?       no
  Should wxWidgets be linked as a shared library?         yes
  Should wxWidgets support Unicode?                       yes (using wchar_t)
  What level of wxWidgets compatibility should be enabled?
                                       wxWidgets 2.8      no
                                       wxWidgets 3.0      yes
  Which libraries should wxWidgets use?
                                       STL                no
                                       jpeg               builtin
                                       png                sys
                                       regex              builtin
                                       tiff               builtin
                                       zlib               sys
                                       expat              sys
                                       libmspack          no
                                       sdl                no


###

make uninstall

make clean

make

make install

/opt/wx/gtk2/3.1.x/bin/wx-config --libs

git://cb.biplab.in/codeblocks.git

cd ~/devel
mkdir codeblocks
cd ~/devel/codeblocks
git clone git://cb.biplab.in/codeblocks.git codeblocks-git

####
# Requires Cygwin packages:
#   libtool 2.4-1
#		automake
#   zip
#   gcc 4.9.2-1 does not work "g++: error: unrecognized command line option '-pthread'"
#   gcc 4.8.3 	seems to be working great.
#      
####


cd ~/devel/codeblocks/codeblocks-git


export ACLOCAL_FLAGS="-I `/opt/wx/gtk2/3.1.x/bin/wx-config --prefix`/share/aclocal"
echo $ACLOCAL_FLAGS

./bootstrap
export CPPFLAGS="-U__STRICT_ANSI__"
export CXXFLAGS="-DWXMAKINGDLL_SCI -Wno-attributes"
echo $CXXFLAGS
 ./configure --enable-shared --enable-compiler --host=i686-pc-cygwin --disable-xpmanifest --disable-pch --with-wx-config=/opt/wx/gtk2/3.1.x/bin/wx-config


#  
# --with-wx-prefix=/opt/wx/gtk2/3.1.x
# --with-platform=PLATFORM #the platform to build, win32, macosx or gtk(default)
# X features:
#  --x-includes=DIR    X include files are in DIR
#  --x-libraries=DIR   X library files are in DIR

make clean

make

make install



In order to run Code::Blocks 
####
# Requires Cygwin packages:
#   These packages where suggested as good ones to add to get GNU Octave to run; I am hoping it workes for getting CB to run.
#   xinit
#   xlaunch
#		??gnuplot
####

$		startxwin /usr/local/bin/codeblocks.exe --prefix="/usr/local"

C:\Apps32\Cygwin1_7\bin\mintty.exe /bin/sh -lc 'startxwin /usr/local/bin/codeblocks.exe --prefix="/usr/local"'




######################################################################

##  git remote add origin  https://stahta01@github.com/stahta01/codeblocks_https_metadata.git
##  git remote -v
##  git branch --list


cd ../codeblocks_PCH_fixes-git && git status -uno
git fetch pch_fixes && git fetch origin && git status -uno
git checkout master && git pull origin master && git status -uno
git svn fetch && git svn info
##  git push 
git checkout win_cbp_saveas && git rebase master && git.exe push pch_fixes --force-with-lease
git checkout wxContribItems && git rebase master && git.exe push pch_fixes --force-with-lease

####

##  git checkout -b portability/fixes pch_fixes/portability/fixes

cd ../codeblocks_PCH_fixes-git
git checkout portability/fixes && git fetch pch_fixes && git status -uno
##  git fetch origin && git rebase origin/bugfix/project_n_workspace
##  git rebase wxContribItems
git rebase win_cbp_saveas && git status -uno
git push pch_fixes 
## git rebase origin/master && git push pch_fixes --force-with-lease

git --no-pager log --oneline --author=Tim

####

##  git checkout -b PCH/cbp_fixes pch_fixes/PCH/cbp_fixes

cd ../codeblocks_PCH_fixes-git
git checkout PCH/cbp_fixes && git status -uno
# PCH_build_fix:
###
# Rebase this complete section after one is added or branch is changed.
# Only add to the top.
git rebase PCH/NassiShneiderman
git rebase PCH/lib_finder
git rebase PCH/keybinder
git rebase PCH/wxSmithAui
git rebase PCH/SmartIndent
git rebase PCH/DoxyBlocks
git rebase PCH/toolsplus
git rebase PCH/FileManager
###
git rebase portability/fixes && git status -uno
git push pch_fixes 
##  git rebase origin/master && git push pch_fixes --force-with-lease

../../../sdk/wxscintilla/include

fixup! - PCH_build_fix: Added search path "sdk/wxscintilla/include". (Thanks stahta01)

fixup! - PCH_build_fix: Added compiler option [-include "sdk.h"]. (Thanks stahta01)
- PCH_build_fix: Added compiler option [-include "sdk.h"]. (Thanks stahta01)

And, added option "-Winvalid-pch".
Also, added object include search folder.

../../../.objs/include

fixup! - PCH_build_fix: Removed define of "BUILDING_PLUGIN". (Thanks stahta01)

This define is NOT needed under Linux.


fixup! - PCH_build_fix: Added object include search folder. (Thanks stahta01)
fixup! - PCH_build_fix: Removed define of "WX_PRECOMP". (Thanks stahta01)
fixup! - PCH_build_fix: Added option "-Winvalid-pch". (Thanks stahta01)
    Also, added object include search folder.

../../../.objs30/include

fixup! - PCH_build_fix: Added define of "CB_PRECOMP". (Thanks stahta01)
    And, removed defines of NOPCH and WX_PRECOMP.

git log -20

git log 0236d6cbdf027db4cc698ba7247a03b73755c4ce

git rebase -i 0236d6cbdf027db4cc698ba7247a03b73755c4ce

git format-patch 0236d6cbdf027db4cc698ba7247a03b73755c4ce

git --no-pager log --oneline --author=Tim 0236d6cbdf027db4cc698ba7247a03b73755c4ce..HEAD

../../../.objs/include

../../.objs/include

../../../.objs30/include

############################
#
#
#

- NassiShneiderman: Fixed PCH issues in code using "sdk.h". (Thanks stahta01)

KeyBinder is broken for NOPCH builds


cd ../codeblocks_PCH_fixes-git
git checkout PCH/BrowseTracker && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease

cd ../codeblocks_PCH_fixes-git
git checkout PCH/SmartIndent && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease


cd ../codeblocks_PCH_fixes-git
git checkout PCH/wxSmithAui && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease



- keybinder: Fixed PCH issues in code using "sdk.h". (Thanks stahta01)

cd ../codeblocks_PCH_fixes-git
git checkout PCH/keybinder && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease


cd ../codeblocks_PCH_fixes-git
git checkout PCH/lib_finder && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes 
##  git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease


cd ../codeblocks_PCH_fixes-git
git checkout PCH/DoxyBlocks && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease


cd ../codeblocks_PCH_fixes-git
git checkout PCH/FileManager && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease

#
Total (2 minute(s), 58 second(s))
Link 13 second(s)


cd ../codeblocks_PCH_fixes-git
git checkout PCH/NassiShneiderman && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes -u
##  git rebase master && git push pch_fixes --force-with-lease

#####

- toolsplus: Fixed PCH issues in code using "sdk.h". (Thanks stahta01)

cd ../codeblocks_PCH_fixes-git
git checkout PCH/toolsplus && git status -uno
git rebase win_cbp_saveas && git status -uno
# PCH_code_fixes:
git push pch_fixes 
##  git rebase master && git push pch_fixes --force-with-lease

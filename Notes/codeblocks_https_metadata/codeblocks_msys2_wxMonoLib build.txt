# Add Option to change shell in GUI         environmentsettingsdlg.cpp
# Use prefix "msys2_cb:"
cd ../codeblocks_msys2_wxMonoLib-git && git fetch origin && git status -uno
#  git checkout msys2/codeblocks
#  git branch --list
git rebase origin/cygwin_support && git status -uno
git push origin
##  git rebase origin/master && git push origin --force-with-lease
git svn fetch && git svn info

git --no-pager log --pretty=format:%s origin/cygwin_support..origin/msys2/codeblocks

* msys2_cb: compiler: DO NOT USE UPSTREAM
* CbLauncher_wx30.cbp: Save-As using CB 15.12RC1. (Thanks stahta01)
* msys2_cb: Changed some of the "\" to "/".
* msys2_cb: DO NOT USE UPSTREAM; Changed "\*" to "/*".
* msys2_cb: Use UnixFilename2 to set value of macro TARGET_COMPILER_DIR.
* msys2_cb: Added UnixFilename2 using added enum cbPathFormat.
* msys2_cb: copy: Replaced copy with CMD_CP macro.
- msys2_cb: mkdir: Use macros CMD_MKDIR and CMD_MKDIR_IGNORERR.
